# Backend Configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
LOG_LEVEL=INFO
DEBUG=false

# Frontend Configuration (Streamlit runs on 8501 by default)
STREAMLIT_SERVER_PORT=8501

# Data paths (relative to backend root)
CODON_TABLE_PATH=data/codon_tables.json
VENDOR_RULES_PATH=data/vendor_rules.yaml

# No secrets needed—this is a local, computational tool only
